# 🗺️ ReSet (**RE**verse engineering **SET**tlement) 프로젝트 로드맵

본 문서는 기존 데이터베이스(SQL Server)에 있는 복잡한 비즈니스 로직을 분석하여, 안전하게 새로운 현대식 프로그램(C#, Java 등)으로 변환하고 그 결과가 정확한지 검증하는 **전체 마일스톤 과정과 일정**을 안내하는 로드맵입니다.

---

## 🎯 프로젝트 최종 목표

본 프로젝트의 최종 목표는 **오래된 데이터베이스 내부 프로그램(Stored Procedure, 이하 SP)을 최신 프로그래밍 언어(C# / Java)로 안전하게 바꾸고, 바뀐 프로그램이 이전과 똑같이 정확하게 작동하는지 100% 검증하는 신뢰성 있는 파이프라인을 구축하는 것**입니다.

*   **지능적인 기존 프로그램 분석**: 복잡한 데이터베이스 코드를 AI와 검증 도구가 자동으로 분석하여 읽기 쉬운 설명서(*_Spec.md)로 만듭니다.
*   **AI 기반의 자동 프로그래밍**: 분석된 설명서를 토대로 AI 개발 도구가 새로운 최신 프로그램 코드를 자동으로 구현합니다.
*   **샌드박스 환경을 통한 완벽한 검증**: 기존 데이터베이스 프로그램과 새로 만든 최신 프로그램에 똑같은 테스트 값을 입력해 보고, 두 결과가 완벽하게 일치하는지 정밀 대조(*_CompareReport.md)하여 오류를 사전에 차단합니다.

---

## 📌 마이그레이션 전체 프로세스

도구를 통해 레거시 데이터베이스 로직을 안전하게 현대화 시스템으로 이관하기 위해 설계된 거시적 데이터 및 제어 프로세스입니다.

```mermaid
flowchart LR
    %% 각 Stage 내부는 세로, Stage 전체는 가로 배치
    subgraph Stage1 ["1단계. 분석 및 문서화"]
        direction TB
        DB로그인["1. DB 접속"] --> DFS의존성추적["2. 연관된 테이블/코드 자동 수집"]
        DFS의존성추적 --> AI분석및교차리뷰["3. AI 분석 및 상호 검토"]
        AI분석및교차리뷰 --> 3단계검증루프["4. 3중 품질 검사"]
        3단계검증루프 --> 명세서저장_Spec["5. 최종 설명서 저장 (*_Spec.md)"]
    end
    
    subgraph Stage2 ["2단계. 프로그램 자동 생성"]
        direction TB
        TUI순차선택["1. 대상 선택"] --> 배치전환계획서작성["2. 순서에 맞게 계획 수립"]
        배치전환계획서작성 --> 통합지시서번들링["3. AI 작업 지시서 작성"]
        통합지시서번들링 --> 코딩에이전트자동기동["4. AI 개발 도구 실행"]
        코딩에이전트자동기동 --> 타겟소스코드생성["5. 최신 프로그램 완성"]
    end
    
    subgraph Stage3 ["3단계. 정밀 비교 검증"]
        direction TB
        스마트파일매핑["1. 설명서-프로그램 짝짓기"] --> AI일치성Gap분석["2. 누락되거나 다른 로직 검사"]
        AI일치성Gap분석 --> 테스트케이스_및_모의데이터생성["3. 테스트용 가상 데이터 준비"]
        테스트케이스_및_모의데이터생성 --> Sandbox_DB적재["4. 샌드박스 환경에 데이터 넣기"]
        Sandbox_DB적재 --> 양방향결과덤프수집["5. 양쪽 프로그램 실행 및 결과 수집"]
        양방향결과덤프수집 --> Sandbox_DB소거["6. 샌드박스 환경 청소 (초기화)"]
        Sandbox_DB소거 --> 데이터1대1비교대조["7. 실행 결과 1:1 비교 (수치/날짜 대조)"]
        데이터1대1비교대조 --> 정합성보고서도출_CompareReport["8. 검증 결과 보고서 작성 (*_CompareReport.md)"]
    end

    %% 메인 흐름: Stage들을 가로로 연결
    Start([시작]) --> Stage1 --> Stage2 --> Stage3 --> End([완료])
```

---

## 🗓️ 프로젝트 전체 일정

새로운 최신 시스템으로 안전하게 이전하기 위한 전체 프로젝트 수행 일정 계획입니다. 본 도구의 각 마일스톤(Milestone 1 ~ 6) 개발 및 적용 단계와 매핑되어 진행됩니다.

| 기간 | 주차 | 우리가 하는 일 | 관련 마일스톤 및 산출물 |
| :--- | :---: | :--- | :--- |
| 2026-06-09 ~ 2026-06-18 | 1~2주차 | 프로젝트 개요 정의 및 흐름 기반 로드맵/산출물 규격 설계 | 로드맵 수립 & 산출물 템플릿 정의 |
| 2026-06-19 ~ 2026-07-16 | 3~6주차 | 기존 데이터베이스 프로그램을 정밀 분석하고 AI를 통해 설명서를 작성 및 검토합니다. | **Milestone 1, 2, 3**<br>· 분석 설명서 (`*_Spec.md`)<br>· 캐시 인덱스 (`.sp_cache_index.json`) |
| 2026-07-17 ~ 2026-08-06 | 7~9주차 | 여러 설명서를 하나로 묶어 AI 개발도구를 통해 새로운 최신 프로그램으로 자동 구현합니다. | **Milestone 4**<br>· 통합 전환 계획서 (`*_BatchMigrationPlan.md`)<br>· 통합 작업 지시서 (`{JobName}_MigrationInstructions.md`)<br>· 현대화 완료 소스코드 |
| 2026-08-07 ~ 2026-08-27 | 10~12주차 | 작성된 설명서와 실제 구현된 프로그램의 비즈니스 규칙이 서로 일치하는지 비교합니다. | **Milestone 5**<br>· 논리 흐름 비교 보고서 (`GapReport`) |
| 2026-08-28 ~ 2026-09-24 | 13~16주차 | 샌드박스 환경에 모의 데이터를 넣어 실제 두 프로그램의 결과가 똑같은지 1:1로 비교합니다. | **Milestone 6**<br>· 테스트용 가상 데이터 (`*_mock_data.json`)<br>· 결과 비교 보고서 (`*_CompareReport.md`) |
| 2026-09-25 ~ 2026-11-05 | 17~22주차 | 폐쇄망으로 이전하여 테스트 DB의 실제 데이터로 검증하고 안정화 작업을 거쳐 최종 완료합니다. | 최종 검증 완료 및 이관 |

---

## 🎯 마일스톤별 상세 비즈니스 목표

각 마일스톤의 세부 기술적 메커니즘(DFS 탐색 알고리즘, Actor-Critic 앙상블 합성법, 1:1 결과 정합성 비교 대조 등)은 [architecture.md](file:///home/moondae/git-root/ReSet/docs/architecture.md)에 상세히 기술되어 있으며, 로드맵 상에서는 비즈니스적 마일스톤 목표 위주로 관리됩니다.

### 🗺️ Milestone 1: 기존 시스템 분석 및 수집 (Legacy 분석)
*   **비즈니스 목적**: 레거시 DB에 분산된 SP DDL, 스키마, 한글 설명 및 동적 의존성을 빠짐없이 수집하여 AI 분석가에게 온전한 도메인 지식 컨텍스트를 공급합니다.
*   **비즈니스 가치**: 수동 영향도 파악에 들던 공수를 90% 이상 절감하고 안전한 Soft Fail로 수집 안정성을 담보합니다.

### 🗺️ Milestone 2: 분석 설명서의 3중 신뢰성 검증 및 진행도 시각화
*   **비즈니스 목적**: LLM의 임의적 환각(Hallucination) 현상을 차단하고, 병렬 API 수행(Low/Medium/High) 상태를 실시간 프로그레스 바(TUI Progress)로 보여주며 완성도 높은 기능 명세서를 저장합니다.
*   **비즈니스 가치**: Actor-Critic 및 Consolidator 앙상블 모델을 통해 명세서의 왜곡률을 극단적으로 제어하고 사용자에게 실시간 가시성을 전달합니다.

### 🗺️ Milestone 3: 변경 감지기를 통한 비용 최적화 (캐싱)
*   **비즈니스 목적**: 변경되지 않은 SP의 재분석 요청 시 AI 호출을 건너뛰고 기존 보고서를 복원해 요금 낭비를 방지하고, Serilog 기반 파일 로그를 보존합니다.
*   **비즈니스 가치**: API 호출 요금을 절감함과 동시에 로컬망(폐쇄망) 환경에서 증분 검증만을 수행할 수 있는 비용/보안 통제력을 획득합니다.

### 🗺️ Milestone 4: AI와 대화하며 최신 프로그램 자동 생성 (코드 작성)
*   **비즈니스 목적**: 최종 승인된 계획과 스키마 정보들을 담아 하나의 마일스톤 지시서로 번들링하여 외부 코딩 에이전트(Claude Code 등)와 대화하며 소스코드를 자동으로 완성합니다.
*   **비즈니스 가치**: 분석에서 소스코드 작성으로 이어지는 전환 단계를 단절 없이 하나로 연결하여 개발 생산성을 극대화합니다.

### 🗺️ Milestone 5: 설명서와 완성된 프로그램의 불일치 분석 (Gap 분석)
*   **비즈니스 목적**: 구현된 프로그램 소스코드를 정적 구문 linter 및 AI 논리 분석을 거쳐 설계서 규칙과의 일치성을 검사하고 Gap 보고서를 도출합니다.
*   **비즈니스 가치**: 설계 규격을 위반하고 개발자 임의로 코딩된 버그나 로직 누락을 런타임 테스트 이전에 조기에 차단합니다.

### 🗺️ Milestone 6: 샌드박스 환경 기반 실행 결과 대조 (정합성 검증)
*   **비즈니스 목적**: 실제 데이터 오염이나 유출 없이 샌드박스 DB에 모의 데이터(Mock)를 적재/소거하고, 레거시 SP와 타겟 프로그램 실행 결과를 1:1 대조 리포트로 생성합니다.
*   **비즈니스 가치**: 소수점, 날짜 등의 미세한 표현 차이를 보정하며 실제 실행 정합성을 100% 보장하고, 실패 시 3단계 피드백 복구 경로를 통해 빠른 리팩토링 주기를 지원합니다.
